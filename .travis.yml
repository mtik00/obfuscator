language: python
python:
  - "3.3"
  - "2.7"
  - "2.6"
# command to install dependencies, e.g. pip install -r requirements.txt --use-mirrors
install: pip install unittest2 --use-mirrors
# command to run tests, e.g. python setup.py test
script:  python lib/setup.py test
